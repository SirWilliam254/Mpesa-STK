import express from "express"; // handle routes, middleware, templates...
import cors from "cors"; // middleware for handling cross-origin requests


app.listen(5000, () => {
    console.log('server running on port 5000');
});